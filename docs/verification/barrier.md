<!-- GENERATED by backend/tools/gen_verification_docs.py — do not hand-edit. Edit data/verification/*.json instead. -->


# Barrier (Six Gods' Blessing)

- **Status:** ⚠️ Unverified
- **Mechanic tags:** defense, barrier, absorb

## Setup

Activate Barrier (the Barrier Active condition), optionally with Barrier Shield and/or Barrier Absorption Rate modifiers. The Barrier panel appears on the Player Stats screen only while Barrier is active.

## Raw data points

Sourced from the TLI Help Database, not yet confirmed live. Barrier absorbs Hit / Secondary / Reflected damage equal to 20% of (Max Life + Max Energy Shield), at a base absorption rate of 50%. Base duration 10s; a new Barrier can't be obtained while one is active.

## Derived / confirmed formula

barrier_shield = 0.20 · (Max Life + Max Energy Shield) · (1 + barrier_shield_inc) · Π(1 + barrier_shield_additional); barrier_absorption_rate = min(0.50 · (1 + barrier_absorption_rate_inc), 1.0).

## Notes / caveats / open questions

Pool value + rate are screenshot-verifiable against the in-game Defense panel. Open: how the barrier pool ABSORBS an incoming hit (the absorption-rate application order in the mitigation chain) is deferred to the incoming-damage / EHP model (WS3). Absorption rate capped at 100% as a sane bound — not stated in the Help DB.

## Implementation (engine model)

engine/defense.py::_barrier + calculate_defense (barrier_shield / barrier_absorption_rate / barrier_active on DefenseResult; barrier stats now consumed). UI: PlayerStatsScreen.tsx renders a dedicated Barrier panel (conditional on barrier_active), replacing the old Absorb 'Barrier — NYI' stub. The barrier_active condition is declared in data/conditions.json (mirrors blur_active).

## Sources

- TLI Help Database — Battle Mechanics/Statuses/Six Gods' Blessings/Barrier.md
- data/master_glossary.json — Barrier

# Web build config (npm run build:web / --mode web).
# Copy to `.env.web` and set the CDN base for the static catalogs + engine-data.zip
# (Cloudflare Pages). The value below is the current public default — not a secret.
VITE_STATIC_DATA_BASE=https://tlibuilder-data.pages.dev
